name: "Spike"

on:
  # push:
  #   branches: [main]
  # pull_request:
  #   branches: [main]

  workflow_dispatch:

jobs:
  Spike:
    runs-on: ubuntu-latest
    steps:
      - name: Set the value
        id: step_one
        run: |
          echo "foo=bar" >> $GITHUB_ENV

      - name: Use the value
        id: step_two
        run: |
          echo "${{ env.foo }}"
          echo "${{ env }}"
          printenv